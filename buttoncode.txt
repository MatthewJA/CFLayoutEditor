'''

        # 8 buttons for the different tabs
        self.buttons = {}
        for ii, i in enumerate(['Overall', 'Blog', 'Comic', 'Archive', 'Overview', 'Errors', 'Search', 'CSS']):
            self.buttons[i.lower()] = Button(master, text=i,
                                             command=lambda i=i.lower(): self.switchtab(i))
            self.buttons[i.lower()].grid(row=0, column=ii)

        self.activescreen = 'overall'
        
    def new(self):
        pass
    def upload(self):
        pass
    def save(self):
        pass
    def switchtab(self, i):
        self.buttons[self.activescreen].config(relief=RAISED)
        self.buttons[i].config(relief=SUNKEN)
        self.activescreen = i
'''